#Build Change notes on rageBait.py
(3/16/2025) 0.5
Created a "Door" to move between the levels by creating a door class. Added the mode selection
from the demo code file. Converted that code into a class system to match the new code layout

(3/12/2026) 0.4
made the platform move back and forth across the screen added trap platforms and failing platforms.





(3/4/2026) Ver 0.3
Created a main menu system that makes use of the .collidepoint(mousePos) and mouseClick[0] to "start" , "quit" 
or "level selection". Level selection still needs to be converted to a class. Started thinking on how to make 
the code cleaner and keep it from breaking down as the code gets more complex. *Converted the levels into classes*.
Moved the movement into the player class as a function <def movement(self):>
to be reused across levels. the game now has Gravity which can be found 
in the Player class and changed up how the collision detections works. ground is no longer considered 
different from a platform and is now considered its own platform known as "ground" and the floating platform 
is known as "platform". The level selection works almost identical to the main menu class.
<def handleEvents(self, events):> handles mouse postion and mouse input. IF you are adding new levels you
have to make sure you go into <class LevelSelection:> and in <def __init__(self):> add 
self.LVL3_RECT = pygame.Rect(x, y, width, height). In the event handler you will need to create a new
if statement like this < if self.LVL3_RECT.collidepoint(mousePos) and mouseClick[0]: > new line < return "level3"
it is important that you remember the string used for later when you make the LvlThree class and when you update
the main_menu(): function to update the currentState variable. It is good practice to name the classes in 
PascalCase hence why the already established classes are <LvlOne()> in the <def draw(self):> function in the Level
selection class make sure to draw the new rect with this example code replacing parts to match what you are trying
to add <pygame.draw.rect(SCREEN, WHITE, self.LVL3_RECT)> then you'll need to render the font 
<lvl3TEXT = FONT.render("Level 3", True, Black)> then make sure to actually draw it onto the screen with
<SCREEN.blit(lvl3TEXT, (self.LVL3_RECT.x + 20, self.LVL3_RECT.y + 35))> That should create the level 3 box for
the user to click in the level selector screen and once the currentState and LvlThree() class are created and 
updated should load the LvlThree() class as the currentState.


#Build change notes on rageBait.py
(2/9/2026)
using pygame.sprite.Sprite
when using pygame sprites will always expect to have super().__init__()
Pygame sprites always use .rect.x to track position






For movement control currently using(2/9/2026):
-----------------------------------------------------------------------
#Keybinding
        for event in pygame.event.get():
                if event.type == pygame.QUIT:
                        isRunning = False
        if event.type == pygame.KEYDOWN:
                if event.key == pygame.K_RIGHT:
                        PLAYER_X += PLAYER_SPEED
                if event.key == pygame.K_LEFT:
                        PLAYER_X -= PLAYER_SPEED
                if event.key == pygame.K_UP and player_velocity_y == 0:
                        player_velocity_y = -30
                if event.key == pygame.K_DOWN:
                        PLAYER_Y += PLAYER_SPEED
--------------------------------------------------------------------------
This does work for now and allows movement but it is very choppy and not smooth
Below is an updated movement code taken from the ExampleCode.py file found in the github rep "Game_Files"
--------------------------------------------------------------------------------
#Key control
        keys = pygame.key.get_pressed()
        if keys[pygame.K_LEFT] and p1_x > 0:
            p1_x -= PLAYER_SPEED
        if keys[pygame.K_RIGHT] and p1_x + PLAYER_WIDTH < SCREEN_WIDTH:
            p1_x += PLAYER_SPEED
        if keys[pygame.K_SPACE] and not isJumping:
            p1_velocity_y = -400 * delta_time
            isJumping = True
----------------------------------------------------------------------------
Updated the base code to have collision, working gravity, and jumping
