#Build Change notes on rageBait.py
(3/4/2026) Ver 0.3
Created a main menu system that makes use of the .collidepoint(mousePos) and mouseClick[0] to "start" , "quit" or "level selection". Level selection still needs to be converted to a class. Started thinking on how to make the code cleaner and keep it from breaking down as the code gets more complex. *Converted the levels into classes*. Moved the movement into the player class to be reused across levels. the game now has Gravity which can be found in the Player class and changed up how the collision detections works. ground is no longer considered different from a platform and is now considered its own platform known as "ground" and the floating platform is known as "platform"

#Build change notes on rageBait.py
(2/9/2026)
using pygame.sprite.Sprite
when using pygame sprites will always expect to have super().__init__()
Pygame sprites always use .rect.x to track position






For movement control currently using(2/9/2026):
-----------------------------------------------------------------------
#Keybinding
        for event in pygame.event.get():
                if event.type == pygame.QUIT:
                        isRunning = False
        if event.type == pygame.KEYDOWN:
                if event.key == pygame.K_RIGHT:
                        PLAYER_X += PLAYER_SPEED
                if event.key == pygame.K_LEFT:
                        PLAYER_X -= PLAYER_SPEED
                if event.key == pygame.K_UP and player_velocity_y == 0:
                        player_velocity_y = -30
                if event.key == pygame.K_DOWN:
                        PLAYER_Y += PLAYER_SPEED
--------------------------------------------------------------------------
This does work for now and allows movement but it is very choppy and not smooth
Below is an updated movement code taken from the ExampleCode.py file found in the github rep "Game_Files"
--------------------------------------------------------------------------------
#Key control
        keys = pygame.key.get_pressed()
        if keys[pygame.K_LEFT] and p1_x > 0:
            p1_x -= PLAYER_SPEED
        if keys[pygame.K_RIGHT] and p1_x + PLAYER_WIDTH < SCREEN_WIDTH:
            p1_x += PLAYER_SPEED
        if keys[pygame.K_SPACE] and not isJumping:
            p1_velocity_y = -400 * delta_time
            isJumping = True
----------------------------------------------------------------------------
Updated the base code to have collision, working gravity, and jumping
